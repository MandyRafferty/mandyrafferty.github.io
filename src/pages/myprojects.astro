---
import Layout from '../components/Layout.astro';
import ProjectCard from '../components/ProjectCard.astro';

const base = import.meta.env.BASE_URL;
const mdProjGlob = import.meta.glob('../content/myprojects/*.md', { eager: true });
const mdxProjGlob = import.meta.glob('../content/myprojects/*.mdx', { eager: true });
const allProjects = [...Object.values(mdProjGlob), ...Object.values(mdxProjGlob)] as any[];

// Sort all projects by project_num (ascending order: 1, 2, 3, etc.)
const sortedProjects = allProjects
  .sort((a: any, b: any) => {
    // Sort by project_num - projects without project_num go to end
    const aNum = a.frontmatter.project_num || 999;
    const bNum = b.frontmatter.project_num || 999;
    return aNum - bNum;
  });

// Group projects by category
const engineeringProjects = sortedProjects.filter(project => 
  project.frontmatter.category === 'engineering'
);
const diyProjects = sortedProjects.filter(project => 
  project.frontmatter.category === 'diy'
);

const ewbProject = allProjects.find(project => 
  project.file.includes('ewb-kenya.md')
);
const ewbSlug = ewbProject?.file.split('/').pop()?.replace(/\.(md|mdx)$/, '');
---

<Layout title="Projects" description="All projects.">
  <div class="project-page">
    <section class="demo-section">
      <div class="container">
        <h1 class="section-title">Browse Projects by Category</h1>
        <!-- <p class="project-description">
          Browse projects by category.
        </p> -->
        <hr class="project-rule" />

        <!-- Engineering Projects Section -->
        <div class="project-card">
          <div class="card-image">
            <a href={`${base}engineering-projects/`}>
              <img src="/images/granJamTestingThumbnail.jpg" alt="Engineering Projects" />
            </a>
          </div>
          <div class="card-content">
            <a href={`${base}engineering-projects/`}>
              <h2>Engineering Projects</h2>
            </a>
            <p>
              Projects completed while studying biomedical engineering at Rutgers University and mechanical engineering at Carnegie Mellon University.
            </p>
            <a href={`${base}engineering-projects/`} class="btn btn-primary" style="align-self: center;">Learn More</a>
          </div>
        </div>

        <!-- DIY Design & Fabrication Section -->
        <div class="project-card">
          <div class="card-image">
            <a href={`${base}DIYdesign/`}>
              <img src="/images/DIYprojects-thumbnail.jpg" alt="DIY Design & Fabrication" />
            </a>
          </div>
          <div class="card-content">
            <a href={`${base}DIYdesign/`}>
              <h2>DIY Design & Fabrication</h2>
            </a>
            <p>
              Mechanical engineering master's level course at Carnegie Mellon University that focuses on product design and fabrication.
            </p>
            <a href={`${base}DIYdesign/`} class="btn btn-primary" style="align-self: center;">Learn More</a>
          </div>
        </div>

        <!-- EWB -->
        <div class="project-card">
          <div class="card-image">
            <a href={`${base}myprojects/${ewbSlug}/`}>
              <img src="/images/EWBgroupphoto.jpg" alt="Engineers Without Borders" />
            </a>
          </div>
          <div class="card-content">
            <a href={`${base}myprojects/${ewbSlug}/`}>
              <h2>Engineers Without Borders: Project Kenya</h2>
            </a>
            <p>
              Project Kenya's water project as a part of the EWB Rutgers University Chapter,
              which focused on increasing clean water accessibility for a community in Kolunje, Kenya.
            </p>
            <a href={`${base}myprojects/${ewbSlug}/`} class="btn btn-primary" style="align-self: center;">Learn More</a>
          </div>
        </div>

<!-- Personal -->
        <div class="project-card">
          <div class="card-image">
            <a href={`${base}personal-projects/`}>
              <img src="/images/Handbag/handbag-thumbnail.jpg" alt="Personal Projects" />
            </a>
          </div>
          <div class="card-content">
            <a href={`${base}personal-projects/`}>
              <h2>Personal Projects</h2>
            </a>
            <p>
              Personal explorations in jewelry making, sewing, and handmade crafts that blend 
              creativity with hands-on fabrication.
            </p>
            <a href={`${base}personal-projects/`} class="btn btn-primary" style="align-self: center;">Learn More</a>
          </div>
        </div>
      </div>
    </section>

    <!-- Test -->
<!-- <article class="project-card">
  {ewbProject.thumbnail && (
    <div class="project-thumbnail">
      <img src={ewbProject.thumbnail} alt="Group photo" />
    </div>
  )}

  <div class="project-content">
    <h3 class="project-title">
      <a href={`${base}myprojects/${ewbSlug}/`}>Engineers Without Borders: Project Kenya</a>
    </h3>

    {ewbProject.excerpt && (
      <p class="project-excerpt">{ewbProject.excerpt}</p>
    )} -->

    <!-- Using existing class -->
<!-- <article class="project-card">
  {ewbProject.thumbnail && (
    <div class="project-thumbnail">
      <img src={ewbProject.thumbnail} alt={ewbProject.title} />
    </div>
  )}

  <div class="project-content">
    <h3 class="project-title">
      <a href={`${base}myprojects/${ewbSlug}`}>{ewbProject.title}</a>
    </h3>

    {ewbProject.excerpt && (
      <p class="project-excerpt">{ewbProject.excerpt}</p>
    )}
  </div>
</article> -->

    <div class="hero-actions">
      <a href={`${base}allprojects/`} class="btn btn-primary">View All Projects</a>
    </div>
  </div>
</Layout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--content-padding);
  }

  .demo-section {
    padding: calc(var(--grid-unit) * 8) 0;
  }

  .section-title {
    font-family: var(--font-heading-secondary);
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--color-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: calc(var(--grid-unit) * 6);
    text-align: center;
  }

  .project-page {
    max-width: 1200px;
    margin: 0 auto;
  }

  .project-description {
    font-size: 1.25rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    margin-bottom: calc(var(--grid-unit) * 4);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    text-align: center;
  }

  .project-rule {
    height: 4px;
    background-color: var(--color-accent);
    border: none;
    width: 120px;
    margin: 0 auto calc(var(--grid-unit) * 8);
  }

  .project-card {
    display: grid;
    grid-template-columns: 1.3fr 1fr;
    gap: calc(var(--grid-unit) * 6);
    align-items: center;
    padding: calc(var(--grid-unit) * 3);
    background-color: var(--color-surface);
    border-radius: 8px;
    margin-bottom: calc(var(--grid-unit) * 8);
    transition: transform 0.2s ease;
  }

  .project-card:hover {
    transform: translateY(-2px);
  }

  .project-card:last-child {
    margin-bottom: 0;
  }

  .card-image {
    overflow: hidden;
    border-radius: 8px;
  }

  .card-image img {
    width: 100%;
    height: 250px;
    object-fit: cover;
    transition: transform 0.3s ease;
    margin: 0;
    border: none;
    border-radius: 8px;
  }

  .project-card:hover .card-image img {
    transform: scale(1.05);
  }

  .card-content {
    display: flex;
    flex-direction: column;
    gap: calc(var(--grid-unit) * 3);
  }

  .card-content h2 {
    margin: 0;
  }

  .card-content p {
    margin: 0;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .section-title {
      font-size: 2rem;
    }

    .project-description {
      font-size: 1.125rem;
    }

    .project-card {
      grid-template-columns: 1fr;
      gap: calc(var(--grid-unit) * 4);
      text-align: center;
    }
  }

  @media (max-width: 480px) {
    .demo-section {
      padding: calc(var(--grid-unit) * 4) 0;
    }

    .project-card {
      padding: calc(var(--grid-unit) * 3);
    }

    .card-image img {
      height: 200px;
    }
  }
</style>